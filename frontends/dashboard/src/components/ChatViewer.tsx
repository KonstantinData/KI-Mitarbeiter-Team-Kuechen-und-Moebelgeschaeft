/** Anzeige einer Chat-Konversation. Platzhalter — wird spaeter implementiert. */

interface ChatViewerProps {
  // Props werden hier ergaenzt
  [key: string]: unknown;
}

export function ChatViewer(_props: ChatViewerProps) {
  return <div className="text-gray-400 text-sm">ChatViewer (Platzhalter)</div>;
}
