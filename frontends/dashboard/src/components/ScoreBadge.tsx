/** Badge fuer Lead-Score. Platzhalter — wird spaeter implementiert. */

interface ScoreBadgeProps {
  // Props werden hier ergaenzt
  [key: string]: unknown;
}

export function ScoreBadge(_props: ScoreBadgeProps) {
  return <div className="text-gray-400 text-sm">ScoreBadge (Platzhalter)</div>;
}
