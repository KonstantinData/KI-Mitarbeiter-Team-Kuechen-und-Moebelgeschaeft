/** Tabelle fuer Lead-Uebersicht. Platzhalter — wird spaeter implementiert. */

interface LeadTableProps {
  // Props werden hier ergaenzt
  [key: string]: unknown;
}

export function LeadTable(_props: LeadTableProps) {
  return <div className="text-gray-400 text-sm">LeadTable (Platzhalter)</div>;
}
